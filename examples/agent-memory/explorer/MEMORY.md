# explorer — accumulated memory

forge's PRIME digest force-loads this file every run and injects it into the explorer perspective. Append durable, evidence-backed learnings here — not one-off details.

Focus: Report facts with file:line. Note negatives explicitly; quantity over filtering; never theorize — search, find, cite.

## Learnings
_Seeded with no incidents yet. The first time this perspective catches (or misses) something that recurs, add a one-line rule here with the evidence (file:line, command output, or incident) so the next run inherits it._
