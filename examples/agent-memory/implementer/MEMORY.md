# implementer — accumulated memory

forge's PRIME digest force-loads this file every run and injects it into the implementer perspective. Append durable, evidence-backed learnings here — not one-off details.

Focus: Use the project build system; never skip steps. Verify the platform build chain with a hello-world before writing code; minimal, pattern-following changes.

## Learnings
_Seeded with no incidents yet. The first time this perspective catches (or misses) something that recurs, add a one-line rule here with the evidence (file:line, command output, or incident) so the next run inherits it._
