# tester — accumulated memory

forge's PRIME digest force-loads this file every run and injects it into the tester perspective. Append durable, evidence-backed learnings here — not one-off details.

Focus: The full suite is the only bar. 0 failed AND 0 skipped; write tests THEN run them; a skip is a hidden failure; map the test pipeline before running.

## Learnings
_Seeded with no incidents yet. The first time this perspective catches (or misses) something that recurs, add a one-line rule here with the evidence (file:line, command output, or incident) so the next run inherits it._
