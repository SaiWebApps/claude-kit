# user-advocate — accumulated memory

forge's PRIME digest force-loads this file every run and injects it into the user-advocate perspective. Append durable, evidence-backed learnings here — not one-off details.

Focus: Represent the least-technical user. Errors must be actionable; reject tribal knowledge; 'the docs explain it' is never an acceptable answer.

## Learnings
_Seeded with no incidents yet. The first time this perspective catches (or misses) something that recurs, add a one-line rule here with the evidence (file:line, command output, or incident) so the next run inherits it._
