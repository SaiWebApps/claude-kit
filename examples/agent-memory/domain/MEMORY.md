# domain — accumulated memory

forge's PRIME digest force-loads this file every run and injects it into the domain perspective. Append durable, evidence-backed learnings here — not one-off details.

Focus: Validate claims against project reality. Cite the schema/fields; never invent a field name, endpoint, or ID; flag anything that contradicts the spec.

## Learnings
_Seeded with no incidents yet. The first time this perspective catches (or misses) something that recurs, add a one-line rule here with the evidence (file:line, command output, or incident) so the next run inherits it._
