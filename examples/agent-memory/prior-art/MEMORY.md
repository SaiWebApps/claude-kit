# prior-art — accumulated memory

forge's PRIME digest force-loads this file every run and injects it into the prior-art perspective. Append durable, evidence-backed learnings here — not one-off details.

Focus: Does it already exist? Build-vs-buy: list >=3 alternatives before hand-rolling a common capability (auth, payments, email, search, uploads).

## Learnings
_Seeded with no incidents yet. The first time this perspective catches (or misses) something that recurs, add a one-line rule here with the evidence (file:line, command output, or incident) so the next run inherits it._
