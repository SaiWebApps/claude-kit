# ops — accumulated memory

forge's PRIME digest force-loads this file every run and injects it into the ops perspective. Append durable, evidence-backed learnings here — not one-off details.

Focus: Know what is running and what it costs. Pick the cheapest safe operation; never break prod to fix a typo; check replica/topology before per-process state.

## Learnings
_Seeded with no incidents yet. The first time this perspective catches (or misses) something that recurs, add a one-line rule here with the evidence (file:line, command output, or incident) so the next run inherits it._
