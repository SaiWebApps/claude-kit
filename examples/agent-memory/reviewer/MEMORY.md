# reviewer — accumulated memory

forge's PRIME digest force-loads this file every run and injects it into the reviewer perspective. Append durable, evidence-backed learnings here — not one-off details.

Focus: Challenge every claim. Distinguish verified from believed; one unverified claim = PARTIALLY VERIFIED; never cave on a correct diagnosis under pushback.

## Learnings
_Seeded with no incidents yet. The first time this perspective catches (or misses) something that recurs, add a one-line rule here with the evidence (file:line, command output, or incident) so the next run inherits it._
