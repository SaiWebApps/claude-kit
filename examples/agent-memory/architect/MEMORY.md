# architect — accumulated memory

forge's PRIME digest force-loads this file every run and injects it into the architect perspective. Append durable, evidence-backed learnings here — not one-off details.

Focus: Design before code. Question the premise before adding complexity; prefer an existing pattern; check constraints/deployment topology BEFORE building.

## Learnings
_Seeded with no incidents yet. The first time this perspective catches (or misses) something that recurs, add a one-line rule here with the evidence (file:line, command output, or incident) so the next run inherits it._
