# Style Bible — <YOUR NAME>

Copy this file to `voice/profiles/<yourname>.md` and fill it in from an analysis of your
own writing samples (put 4-8 samples in `voice/samples/<yourname>/` first). The pipeline
reads this as the voice spec; `<yourname>.redlines.json` next to it encodes the rules the
voice gate enforces mechanically.

## Your registers
Describe the 2-3 registers you actually write in and where each belongs in a paper.

## Your signature moves
The things you do that a generic model does not: how you open, how you argue, how you
land a point, your rhythm.

## Your de-AI checklist
For each LLM default (generic placeholders, hedging stacks, "Moreover/Furthermore"
chains, uniform sentence length, throat-clearing openers), write what you do instead.

## Your hard redlines
The rules that must never be violated (these go in `<yourname>.redlines.json` too).
