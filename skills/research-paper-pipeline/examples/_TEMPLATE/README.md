# Paper template

Copy this folder to start a new paper. Edit `paper.config.json` (set the target
`profile` and your `voice`), replace the `content/` drafts with your sections, and add
each citation to `config/notes.json`. Then from the skill root:

```bash
PAPER_DIR=/path/to/your/paper  scripts/build_submission.sh
```
